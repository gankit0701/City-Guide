<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/home_menu"
            android:title="Home"
            android:icon="@drawable/home_icon"/>

        <item
            android:id="@+id/search_menu"
            android:title="Search"
            android:icon="@drawable/search_icon"/>

        <item
            android:id="@+id/all_categories_menu"
            android:title="All Categories"
            android:icon="@drawable/all_categories_icon"/>

        <item
            android:id="@+id/add_missing_menu"
            android:title="Add Missing Place"
            android:icon="@drawable/missing_place_icon"/>




    </group>

    <item android:title="Profile">
        <menu>
            <group android:checkableBehavior="single"
                >
                <item
                    android:id="@+id/menu_login"
                    android:title="Login"
                    android:icon="@drawable/home_icon"/>

                <item
                    android:id="@+id/menu_profile"
                    android:title="Profile"
                    android:icon="@drawable/search_icon"/>

                <item
                    android:id="@+id/menu_logout"
                    android:title="Logout"
                    android:icon="@drawable/all_categories_icon"/>
            </group>
        </menu>

    </item>

</menu>